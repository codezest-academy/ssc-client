# In local development, leave this as /api/v1 because next.config.ts proxies requests to avoid CORS.
# In production, set this to the full URL (e.g., https://api.codezest.in/api/v1)
NEXT_PUBLIC_API_URL=/api/v1
